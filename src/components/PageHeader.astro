---
interface Props {
  title: string;
  class?: string;
}

const { title, class: className = '' } = Astro.props;
---

<header class:list={["mb-12", className]}>
  <h1 class="text-3xl md:text-4xl font-bold tracking-tight text-primary mb-4">{title}</h1>
  <slot />
</header>
