<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <defs>
    <!-- Gradiente sutil para dar profundidade -->
    <radialGradient id="eyeGrad" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#dcdcdc"/>
    </radialGradient>
  </defs>

  <!-- Fundo transparente -->
  
  <!-- Contorno do olho -->
  <path d="M8 32C14 18 50 18 56 32C50 46 14 46 8 32Z"
        fill="url(#eyeGrad)"
        stroke="#111"
        stroke-width="3"
        stroke-linejoin="round"/>

  <!-- Íris -->
  <circle cx="32" cy="32" r="12" fill="#111"/>

  <!-- Botão de play -->
  <polygon points="29,24 29,40 42,32"
           fill="#ffffff"/>
</svg>
