---
import Layout from '../../layouts/Layout.astro';
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import SectionHeader from "../../components/SectionHeader.astro";
import PageHeader from "../../components/PageHeader.astro";
import Subtitle from "../../components/Subtitle.astro";
import Callout from "../../components/Callout.astro";
import BinarySearchVisualizer from '../../components/visualizer/BinarySearchVisualizer';
import { Search } from '@lucide/astro';
---

<Layout title="Busca Binária - Algoritmos">
  <Header />
  <main class="px-4 py-8 max-w-4xl mx-auto">
    
    <PageHeader title="Busca Binária">
      <Subtitle>
        A <span class="text-primary font-medium">busca binária</span> é um algoritmo utilizado para encontrar elementos de forma extremamente eficiente em <span class="text-primary font-medium">arrays ordenados</span>.
      </Subtitle>
      <Subtitle class="mt-4">
        Diferente da busca linear, que percorre os elementos um a um, a busca binária utiliza a <span class="text-primary font-medium">ordem dos dados</span> para reduzir drasticamente a quantidade de comparações necessárias.
      </Subtitle>
    </PageHeader>

    <section class="mb-12" aria-label="Visualização interativa da busca binária">
      <BinarySearchVisualizer client:load />
    </section>

    <section class="mb-12 scroll-mt-20">
      <SectionHeader label="Conceito" title="Como funciona">
        <Search slot="icon" class="size-3" />
      </SectionHeader>
      <div class="text-muted-foreground text-sm space-y-3 text-justify">
        <p>
          A ideia central da busca binária é simples: <span class="text-primary font-medium">sempre olhar para o elemento do meio da estrutura</span>.
        </p>
        <p>
          A cada passo:
        </p>
        <ol class="list-decimal list-inside space-y-2 ml-2">
          <li>Verificamos o valor do elemento central</li>
          <li>Se ele for o valor procurado, a busca termina</li>
          <li>Se o valor procurado for <span class="text-primary font-medium">menor</span>, continuamos a busca apenas na metade esquerda</li>
          <li>Se for <span class="text-primary font-medium">maior</span>, continuamos apenas na metade direita</li>
        </ol>
        <p>
          Como o array está ordenado, metade dos elementos pode ser descartada imediatamente. Em outras palavras, <span class="text-primary font-medium">a cada iteração da busca binária o número de possibilidades é reduzido pela metade</span>.
        </p>
      </div>
    </section>

    <section class="mb-12 scroll-mt-20">
      <SectionHeader label="Exemplo" title="Exemplo">
        <Search slot="icon" class="size-3" />
      </SectionHeader>
      <div class="text-muted-foreground text-sm space-y-3 text-justify">
        <p>
          Considere o seguinte array ordenado:
        </p>
        <pre class="font-mono bg-secondary p-3 rounded-lg text-primary overflow-x-auto text-center">[10, 22, 33, 44, 55, 66, 77]</pre>
        <p>
          Se estivermos procurando o valor <span class="text-primary font-medium">44</span>, primeiro verificamos o elemento central.
        </p>
        <p>
          Caso ele não seja o valor desejado, sabemos exatamente <span class="text-primary font-medium">em qual metade do array devemos continuar procurando</span>, descartando todos os outros elementos.
        </p>
        <p>
          Esse processo continua até que o valor seja encontrado ou não restem mais possibilidades.
        </p>
      </div>
    </section>

    <section class="mb-12 scroll-mt-20">
      <SectionHeader label="Análise" title="Número de passos">
        <Search slot="icon" class="size-3" />
      </SectionHeader>
      <div class="text-muted-foreground text-sm space-y-3 text-justify">
        <p>
          A grande vantagem da busca binária aparece quando analisamos <span class="text-primary font-medium">quantos passos são necessários para encontrar um valor</span>.
        </p>
        <p>
          A cada iteração, o número de elementos considerados é reduzido pela metade. Isso significa que o número de passos cresce <span class="text-primary font-medium">muito lentamente</span>, mesmo quando o array aumenta bastante de tamanho.
        </p>
        <p>
          Por exemplo:
        </p>
          <table class="w-full border-collapse mt-4">
            <caption class="sr-only">Número de elementos e máximo de passos na busca binária</caption>
            <thead>
              <tr class="border-b border-border">
                <th scope="col" class="text-left py-2 pr-4 text-primary font-medium">Número de elementos</th>
                <th scope="col" class="text-left py-2 text-primary font-medium">Máximo de passos</th>
            </tr>
          </thead>
          <tbody>
            <tr class="border-b border-border">
              <td class="py-2 pr-4">3</td>
              <td class="py-2 font-mono">2</td>
            </tr>
            <tr class="border-b border-border">
              <td class="py-2 pr-4">8</td>
              <td class="py-2 font-mono">3</td>
            </tr>
            <tr class="border-b border-border">
              <td class="py-2 pr-4">16</td>
              <td class="py-2 font-mono">4</td>
            </tr>
            <tr class="border-b border-border">
              <td class="py-2 pr-4">32</td>
              <td class="py-2 font-mono">5</td>
            </tr>
            <tr>
              <td class="py-2 pr-4">1.024</td>
              <td class="py-2 font-mono">10</td>
            </tr>
          </tbody>
        </table>
        <p class="mt-4">
          Observe que <span class="text-primary font-medium">dobrar o tamanho da estrutura adiciona apenas um passo extra na busca</span>.
        </p>
      </div>
    </section>

    <section class="mb-12 scroll-mt-20">
      <SectionHeader label="Análise" title="Complexidade">
        <Search slot="icon" class="size-3" />
      </SectionHeader>
      <div class="text-muted-foreground text-sm space-y-3 text-justify">
        <p>
          A complexidade da busca binária é:
        </p>
        <Callout variant="metric">O(log N)</Callout>
        <p>
          Isso significa que o número de passos cresce de forma <span class="text-primary font-medium">logarítmica</span> em relação ao tamanho da estrutura.
        </p>
        <p>
          Na prática, isso torna a busca binária extremamente eficiente para grandes volumes de dados.
        </p>
        <Callout variant="warning" label="Requisito fundamental">
          O array deve estar ordenado.
        </Callout>
      </div>
    </section>

  </main>
  <Footer />
</Layout>
